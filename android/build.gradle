// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    
    repositories {
        google()
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.13.0'
        classpath 'com.google.gms:google-services:4.4.4'

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

apply from: "variables.gradle"

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

// Pin build-tools to a version already present in the local SDK
// (this machine only has 34.0.0/36.0.0 installed, not AGP's default 35.0.0,
// and the SDK dir under Program Files isn't writable without admin rights).
subprojects {
    afterEvaluate { proj ->
        if (proj.hasProperty('android')) {
            proj.android.buildToolsVersion = "36.0.0"
        }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
